# NoisySyntheticDataset
Uses SMOTE on Noisy dataset
This code uses Imbalance-learn library to create sythetic dataset with overlaping issue. I then used SMOTE to resample it and used Logistic Regression for classification.
